# Monarch Router Integration Policy

This template is for router-style integrations that choose between paid providers. It does not make Monarch a marketplace.

Use Monarch to test unsafe provider cases before go-live and to prefer lower-risk providers at runtime.
